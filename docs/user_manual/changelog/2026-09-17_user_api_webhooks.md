Group administrators can manage outgoing webhooks with the User API. Bearer-authenticated endpoints can list, create, update, test and delete webhook configurations for groups administered by the API-key user.

The User API documentation now includes a complete endpoint summary, group discovery endpoints, and the webhook event, delivery, payload and security contracts.
