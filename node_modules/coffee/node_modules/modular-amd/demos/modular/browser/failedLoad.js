/*global define */
define([
    "i/don't/exist"
], function (
    findMe
) {
    "use strict";

    alert("Found: " + findMe.message);
}, function () {
    "use strict";

    alert("Not found.");
});
