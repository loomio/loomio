//update activity feed
$("#activity-feed").html("<%= escape_javascript(render :partial => 'activity', :locals =>
{ :discussion => @discussion }) %>")

var modal = $("#description-revision-history")

if(modal.is(":visible")){
  modal.modal('hide')
}
$("#discussion-context").html("<%= escape_javascript(render 'discussion_context')%>")

// calling all the JS needed to have a fully functional interface after DOM replacement
Discussion.enableInlineEdition()
Discussion.seeMoreDescription()
Application.setupPopovers()
