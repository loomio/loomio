Given(/^there is a hidden parent group$/) do
  pending # express the regexp above with the code you wish you had
end

When(/^I load the create subgroup form$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^the 'finding the group' options should metion the parent$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^the 'joining the group' options should mention the parent$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^the 'discussion privacy' options should mention the parent$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^all the joining options should be available$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^both discussion privacy options should be available$/) do
  pending # express the regexp above with the code you wish you had
end

When(/^I select 'only members can see the group'$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^'invitation only' should be exclusively selected$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^'who can add members\?' should show$/) do
  page.should have_content 'Who can add members?'
end

Then(/^'who can add members\?' should hide/) do
  page.should_not have_content 'Who can add members?'
end

Then(/^'only members can see discussions' should be selected$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^'anyone in parent can see discussions' should be disabled$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^there is a visible parent group$/) do
  pending # express the regexp above with the code you wish you had
end

Given(/^I have loaded the create subgroup form$/) do
  pending # express the regexp above with the code you wish you had
end
