# Poll templates

Coordinators can now create and use poll templates — reusable poll configurations that include all settings and options pre-filled.

From the poll creation form, select a saved template instead of building a poll from scratch. Templates save time when your group regularly runs the same type of poll, such as a monthly proposal or a recurring dot vote. Create and manage templates from the group settings page.
