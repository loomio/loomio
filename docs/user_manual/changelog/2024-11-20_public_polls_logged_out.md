# Public polls visible to logged-out users

Polls in public groups are now visible to visitors who aren't signed in. Previously, logged-out visitors could see the thread but not the poll it contained, which was confusing when someone shared a poll link. Now anyone who can view the thread can also see the poll and its results (if results are public).
