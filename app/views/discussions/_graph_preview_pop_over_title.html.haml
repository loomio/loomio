%span.popover-header-title
  =t :proposal
%span{class: css_class_for_close_at(motion)}
  - if motion.close_at
    = "#{time_ago_in_words(motion.close_at)} until close"
.popover-motion-name
  = motion.name
