# Smaller improvements (January)

A collection of smaller fixes and polish items:

- **Pinned closed threads can now be unpinned.** Fixed a bug where a closed thread that was pinned could not be removed from the pin position.
- **Reaction display fixed.** Reactions with very long names or counts no longer break the layout on the thread page.
