# System notice banner

Instance admins can now set a notice that appears at the top of every page for all users.

Configure it under Admin > Settings. The notice supports rich text and can include links. Use it for announcements, maintenance notices, or to point users to important threads. Users can dismiss the notice and it won't reappear until a new one is set.
