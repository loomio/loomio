- nom = poll.decided_voters_count
- dnom = poll.specified_voters_only ? poll.voters_count : poll.group.memberships_count
- pct = (nom.to_f / dnom.to_f * 100).to_i
%p.text-subtitle-2= t('poll_mailer.common.people_have_voted', nom: nom, dnom: dnom , pct: pct)
