#describe 'Proposal directive', ->
  #$scope = {}
  #element = {}
  #proposal = {id: 1, name: 'shall we?'}
  #discussion = {id: 1}

  #beforeEach module 'loomioApp'

  #beforeEach inject ($compile, $rootScope) ->
    #$scope = $rootScope
    #$rootScope.proposal = proposal
    #$rootScope.discussion = discussion
    #element = $compile('<proposal proposal="proposal" discussion="discussion"></proposal>')($rootScope)
    #$rootScope.$digest()
