# Mark all notifications as read

The notifications panel now has a "Mark all as read" button at the top. One click clears all unread notifications. Previously you had to mark each notification individually or wait for them to be marked as read by email.
