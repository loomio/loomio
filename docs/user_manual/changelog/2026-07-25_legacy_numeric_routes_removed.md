# Legacy numeric resource links removed

Legacy links using `/discussions/:id`, `/groups/:id`, and `/c/:id` no longer redirect to discussions, groups, or comments.

Use Loomio's canonical links instead: `/d/:key` for threads, `/g/:key` or `/:handle` for groups, and the thread or poll links generated by Loomio for comments. Canonical poll links remain available at `/p/:key`.
