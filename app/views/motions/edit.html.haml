#edit-motion
  - set_title @group.name, "Edit Proposal"
  =render '/groups/title', group: @group
  %h1 Edit proposal
  =render 'motions/form', form_args: [@motion],
    submit_text: "Update proposal", disable_with: "Updating proposal"
