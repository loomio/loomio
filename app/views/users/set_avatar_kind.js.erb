if ("<%= current_user.avatar_url %>" == "") {
  $('#avatar-preview').html("<%= current_user.initials.gsub('"', "'").html_safe %>");
}
else {
  $('#avatar-preview').html("<%= image_tag(current_user.avatar_url("large")).gsub('"', "'").html_safe %>");
}
