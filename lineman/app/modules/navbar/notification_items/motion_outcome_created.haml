%a.selector-list-item-link{ng-href: '{{event.link()}}'}
  .media-left
    %user_avatar{user: "actor"}
  .media-body
    %span> {{ actor.name }}
    %span{translate:  "notifications.motion_outcome_created" }>
    %span> {{ event.proposal().name }}
