# Fixed: tagging a direct message thread

Adding a tag when starting a direct message thread (a thread with no group) was being blocked with an error. This is now fixed — tags can be added to direct message threads the same way as to threads in a group.
