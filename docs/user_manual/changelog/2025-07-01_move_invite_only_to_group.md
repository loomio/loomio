# Move invite-only threads to a group

Invite-only threads can now be moved into a group.

If a conversation that started outside of any group grows beyond the initial invitees, the thread creator can move it into a group they belong to. The thread becomes visible to all group members, and the group's notification and privacy settings apply from that point forward.
