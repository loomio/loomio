# Request to join prompt

When someone requests to join a closed group, they now see a prompt explaining what to write and why. This helps prospective members write a useful request (e.g., "I'm a member of the board and need access to meeting materials") rather than leaving the message field blank or unclear.

Group coordinators can also set a custom invitation message template that's sent to new invitees alongside the invitation.
