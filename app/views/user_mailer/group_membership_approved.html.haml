= render '/application/mailer_doctype'
%html
  %head
    = stylesheet_link_tag 'email'
  %body
    %p= t("email.group_membership_approved.intro", group_name: @group.name)

    %p= link_to(t("email.view_group"), group_url(@group, @utm_hash))
