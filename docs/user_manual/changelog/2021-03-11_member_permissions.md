# Member permissions expanded

Groups can now give members more responsibility without granting full coordinator access.

- **Members can manage threads.** A new group permission lets members close, pin, or move threads without needing to be a coordinator. Enable it in group settings.
- **Members can delete their own comments.** Members can now discard and restore their own comments. Coordinators and admins can still permanently delete any comment.
