.media-left
  .user-avatar{aria-hidden: 'true'}
    .avatar-image
      %img{alt: "{{match.label.name}}", ng-src: '{{match.image.avatarUrl}}'}
.mentionable-content
  .invitable-name> {{match.label.name}}
  .invitable-subtitle{ng-if: 'match.label.subtitle'} {{match.label.subtitle}}
