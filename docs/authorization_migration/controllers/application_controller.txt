# Authorization migration tracker
# controller = application
# Update each action as its evidence is added. Do not delete route aliases.

[action about_loomio]
routes = GET /about-loomio
action_id = application#about_loomio
status = inventoried
risk = unclassified
authorization_mode = undecided
current_loader =
current_cancan =
target_policy =
target_query =
base_scope =
parent_constraint =
response_type =
response_records =
sensitive_fields =
response_proof =
actors =
dimensions =
baseline_tests =
policy_tests =
request_tests =
negative_tests =
composition_tests =
decision_differences = undecided
review = pending
commit =
verified_at =
notes =

[action bug_tunnel]
routes = POST /bug_tunnel
action_id = application#bug_tunnel
status = inventoried
risk = unclassified
authorization_mode = undecided
current_loader =
current_cancan =
target_policy =
target_query =
base_scope =
parent_constraint =
response_type =
response_records =
sensitive_fields =
response_proof =
actors =
dimensions =
baseline_tests =
policy_tests =
request_tests =
negative_tests =
composition_tests =
decision_differences = undecided
review = pending
commit =
verified_at =
notes =

[action crowdfunding]
routes = GET /crowdfunding_celebration | GET /crowdfunding
action_id = application#crowdfunding
status = inventoried
risk = unclassified
authorization_mode = undecided
current_loader =
current_cancan =
target_policy =
target_query =
base_scope =
parent_constraint =
response_type =
response_records =
sensitive_fields =
response_proof =
actors =
dimensions =
baseline_tests =
policy_tests =
request_tests =
negative_tests =
composition_tests =
decision_differences = undecided
review = pending
commit =
verified_at =
notes =

[action index]
routes = GET /apps/authorized | GET /apps/registered/:id/:slug | GET /apps/registered/:id | GET /apps/registered | GET /contact | GET /d/new | GET /dashboard/:filter | GET /dashboard/direct_discussions | GET /dashboard | GET /discussion_templates/:id/edit | GET /discussion_templates/:id | GET /discussion_templates/browse | GET /discussion_templates/new | GET /email_preferences | GET /g/:key/emails | GET /g/:key/members/requests | GET /g/:key/membership_requests | GET /g/:key/memberships/:username | GET /g/:key/memberships | GET /g/:key/previous_polls | GET /g/:key/settings | GET /g/:key/tags(/:tag_name) | GET /g/new | GET /groups | GET /inbox | GET /p/new(/:type) | GET /poll_templates/:id/edit | GET /poll_templates/:id | GET /poll_templates/browse | GET /poll_templates/new | GET /polls | GET /profile | GET /report | GET /tasks | GET /thread_templates/:id/edit | GET /thread_templates/:id | GET /thread_templates/browse | GET /thread_templates/new | GET /try
action_id = application#index
status = inventoried
risk = unclassified
authorization_mode = undecided
current_loader =
current_cancan =
target_policy =
target_query =
base_scope =
parent_constraint =
response_type =
response_records =
sensitive_fields =
response_proof =
actors =
dimensions =
baseline_tests =
policy_tests =
request_tests =
negative_tests =
composition_tests =
decision_differences = undecided
review = pending
commit =
verified_at =
notes =

[action ok]
routes = GET /favicon.ico | GET /wp-login.php
action_id = application#ok
status = inventoried
risk = unclassified
authorization_mode = undecided
current_loader =
current_cancan =
target_policy =
target_query =
base_scope =
parent_constraint =
response_type =
response_records =
sensitive_fields =
response_proof =
actors =
dimensions =
baseline_tests =
policy_tests =
request_tests =
negative_tests =
composition_tests =
decision_differences = undecided
review = pending
commit =
verified_at =
notes =
