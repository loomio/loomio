# Member-created templates

Groups can now enable a "Members can create templates" setting so regular members, not just admins, can create discussion and poll templates.

Member-created templates are hidden from the general template list by default, pending admin review. Members can edit or delete their own templates. Thread templates are now called discussion templates throughout the interface.
