%h4= t(:'group_volume.notifications')
.btn-group.pull-right
  %button{type: 'button', class:"btn btn-block btn-left btn-default dropdown-toggle", 'data-toggle': "dropdown", 'aria-expanded': false}
    %i.fa.fa-lg.fa-fw{class: icon_class_for_volume(@membership.volume)}
    = t :"volume.#{@membership.volume}"
    %span.caret
  %ul.dropdown-menu.dropdown-menu-right{role: 'menu'}
    - %w[email normal mute].each do |volume|
      %li.dropdown-menu-item
        = link_to [:set_volume, @group, volume: volume], method: :post do
          %i.fa.fa-lg.fa-fw{class: icon_class_for_volume(volume)}
          %b= t(:"volume.#{volume}")
          %br
          = t(:"group_volume.#{volume}_description")
