# Polls linked in threads

When a poll that started on its own poll page is later added to a thread, visiting the poll URL now redirects to the poll's position within the thread rather than showing a standalone poll page. This keeps the conversation and the vote in one place.
