- size = local_assigns.fetch(:size, 36)
- if user.presence
  - if user.thumb_url
    = image_tag user.thumb_url, alt: "#{user.avatar_initials}", class: "base-mailer__avatar-image",  style: "width: #{size}px; height: #{size}px", width: size, height: size
  - else
    %span.base-mailer__avatar-initials{style: "display:inline-block; width: #{size}px; height: #{size}px"}= user.avatar_initials
- else
  %span.base-mailer__avatar-initials{style: "display: inline-block; width: #{size}px; height: #{size}px"} 👤
