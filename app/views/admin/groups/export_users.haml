#h1 export users in given group ids

= form_tag export_users_report_admin_groups_path, method: :get do
  %p
    = label_tag "group ids (space separated)"
    = text_field_tag :group_ids
  %p
    = label_tag "coordinators only"
    = check_box_tag :coordinators
  = submit_tag "get users"
