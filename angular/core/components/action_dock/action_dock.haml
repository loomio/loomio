.action-dock.lmo-flex{layout: "row"}
  .action-dock__action{ng-repeat: "action in actions", ng-if: "action.canPerform()"}
    %reactions_input{model: "model", ng-if: "action.name == 'react'"}
    %md-button.md-button--tiny{ng-if: "action.name != 'react'", ng-click: "action.perform()"}
      %i.material-icons {{ action.icon }}
      %md-tooltip{md-delay: "750"}
        %span{translate: "action_dock.{{ action.name }}"}
