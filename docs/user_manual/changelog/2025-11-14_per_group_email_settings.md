# Per-group email settings

Each group's email notification preferences are now visible and editable from a single page.

Go to your email settings and you'll see every group you belong to listed with its current notification setting. You can change all of them at once, or adjust individual groups. Previously you had to visit each group's page separately to change its email setting.
