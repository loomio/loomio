.poll-mailer-meeting-responses
  %h3= t(:"poll_mailer.common.responses")
  %table
    - @info.poll.stances.with_reason.each do |stance|
      %tr
        %td= render partial: 'poll_mailer/common/avatar', locals: { participant: stance.participant }
        %td.poll-mailer-common-responses__reason
          #{stance.participant.name}:
          = render_rich_text stance.reason
