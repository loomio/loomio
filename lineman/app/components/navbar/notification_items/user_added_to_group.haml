%a.selector-list-item-link{ng-href: 'event.link()}}'}
  .media-left
    %user_avatar{user: "event.actor()"}
  .media-body
    %span{translate: 'notifications.user_added_to_group', translate-value-name: '{{actor.name}}', translate-value-title: '{{event.group().name}}' }
