# Email complaint tracking

The group members panel now shows which members' email addresses have been rejected or marked as spam by their email provider.

Members whose emails are bouncing or have generated spam complaints get an "Email rejected" chip next to their name. The system automatically stops sending notifications to these addresses, preventing your instance's email reputation from being degraded. Instance admins can view complaint counts from the admin panel.
