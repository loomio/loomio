%table
  %tr
    %td= render "avatar", participant: @info.actor
    %td
      %span=raw t(:"poll_mailer.#{@info.poll_type}.header.#{@info.action_name}", actor: @info.actor.name, title: @info.poll.title)
