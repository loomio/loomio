var modal = $("#description-revision-history")

if(modal.is(":hidden")){
  modal.modal
  modal.modal('show')
}

modal.html("<%= j render 'description_history' %>")
MarkdownSetup.render_markdown("#description-revision-history")

modal.find("a.disabled").bind('click', false)
