# Public demo groups

Anyone can now try Loomio instantly without signing up.

The homepage offers a "Try a demo" button that opens a pre-filled demo group. You can explore threads, polls, and group features with example data. No account is needed to view the demo, though creating and voting requires signing up.

This replaced the old group survey flow, which has been removed.
