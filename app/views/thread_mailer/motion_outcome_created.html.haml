= render 'avatar', author: @author
.thread-mailer__subject
  = t(:"email.thread_mailer.outcome_created", who: @motion.outcome_author.name)
  .thread-mailer__outcome
    = render_rich_text(@motion.outcome, true)

= render 'motion_with_positions'
