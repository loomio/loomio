# Admin remove users

Instance admins can now remove users from the admin panel. Go to Admin > Users, select a user, and choose "Remove." This is useful for cleaning up spam accounts or handling data removal requests without needing to use the console.
