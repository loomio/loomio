%p= t('email.mentioned.context', discussion_title: @discussion.title, author_name: @author.name)
= render_rich_text(@text, true)
- if @eventable.respond_to?(:attachments) && @eventable.attachments.any?
  = render "attachments", comment: @eventable
= render "footer", link: polymorphic_url(@eventable)
