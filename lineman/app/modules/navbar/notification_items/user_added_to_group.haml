%a.selector-list-item-link{ng-href: 'event.link()}}'}
  .media-left
    %user_avatar{user: "event.actor()"}
  .media-body
    %span> actor.name }}
    %span{translate:  "notifications.user_added_to_group" }>
    %span> {{ event.group().name }}
