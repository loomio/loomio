# Custom site description

Instance admins can now set a custom description that appears in search engine results and social media previews for the front page.

Configure it under Admin > Site settings. The description also appears as the meta description tag, helping your Loomio instance show up more clearly in search results. Previously this used a fixed default string.
