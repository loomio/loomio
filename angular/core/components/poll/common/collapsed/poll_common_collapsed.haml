.poll-common-collapsed.lmo-card-padding
  %poll_common_preview{poll: "poll"}
  .poll-common-collapsed__content
    .md-subhead.md-primary
      %strong> {{formattedPollType(poll.pollType)}}:
      %span> {{poll.title}}
    .md-body-1.md-secondary
      %span{translate: "poll_common_collapsed.by_who", translate-value-name: "{{poll.author().name}}"}>
      %span>·
      %poll_common_closing_at{poll: "poll"}
