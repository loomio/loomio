%span.popover-header-title
  =t :proposal
%span{class: css_class_for_close_date(motion)}
  - if motion.close_date
    = time_ago_in_words(motion.close_date) + t(:until_close)
.popover-motion-name
  = motion.name
