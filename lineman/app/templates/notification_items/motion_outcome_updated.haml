%a.selector-list-item-link{ng-href: '{{notification.event().link()}}'}
  .media-left
    %user_avatar{user: "notification.event().actor()"}
  .media-body
    %span> {{ notification.event().actor().name }}
    %span{translate:  "notifications.motion_outcome_updated" }>
    %span> {{ notification.event().proposal().name }}
