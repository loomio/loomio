var modal = $("#description-revision-history")

if(modal.is(":hidden")){
  modal.modal
  modal.modal('show')
}

modal.html("<%= escape_javascript(render 'description_history', model: @discussion, model_name: 'discussion')%>")
modal.find("a.disabled").bind('click', false)
