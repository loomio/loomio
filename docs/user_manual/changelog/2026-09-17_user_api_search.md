## Search is available through the User API

Integrations using a User API key can search visible discussions, comments, polls, votes, and outcomes without downloading and scanning every record. Search follows the API-key user's topic permissions and includes public content without requiring group membership.
