# Admin group privacy settings

Instance admins can now edit a group's visibility setting from the admin panel.

Go to Admin > Groups, select a group, and change its privacy level (open, closed, or secret). Previously, only group coordinators could change this, making it difficult for instance admins to manage groups that had lost their coordinators.
