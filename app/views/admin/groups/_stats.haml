%div{ class: 'panel' }
  %h3 Group Stats
  %div{ class: 'panel-contents' }
    %table{ id: 'stats-table' }
      %tr
        %th Visits
        %th Members
        %th Threads
        %th Subgroups
        %th Polls
        %th Votes
      %tr
        %th= Queries::AdminGroupPage.visits(group.id).last[:count]
        %th= group.memberships_count
        %th= group.discussions_count
        %th= group.subgroups_count
        %th= group.polls_count
        %th= group.polls.reduce(0) { |sofar, poll| sofar + poll.stances.count }
