.poll-proposal-results
  %h3= t(:"poll_mailer.common.responses")
  - @info.poll.grouped_stance_choices.each do |poll_option, stance_choices|
    .poll-proposal-results__poll-option{style: "border-color: #{proposal_colors[poll_option.name]}"}
      %h4= poll_option.name.humanize
      %table
        - stance_choices.each do |stance_choice|
          %tr
            %td= render partial: 'poll_mailer/common/common_avatar', locals: { participant: stance_choice.stance.participant }
            %td.poll-proposal-results__reason= stance_choice.stance.reason
