# Thread sidebar redesign

The in-thread sidebar now shows pinned proposals with participant avatars inline at the top of the navigation list, rather than requiring a separate lookup. The unread count is shown next to the "unread" jump-to label, and navigating to an item now gives a brief highlight animation so it's easy to spot where you landed.
