#new-motion
  =link_to @motion.group.name, group_path(@motion.group), title: "group"
  %h2
    New motion
  =render partial: 'form',
    locals: {form_args: [@motion, {url: motions_path(group_id: @motion.group)}]}
