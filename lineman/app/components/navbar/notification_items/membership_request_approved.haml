%a.selector-list-item-link{ng-href: '{{event.link()}}'}
  .media-left
    %user_avatar{user: "actor"}
  .media-body
    %span{translate: 'notifications.membership_request_approved', translate-value-name: '{{actor.name}}', translate-value-title: '{{event.group().name}}' }
