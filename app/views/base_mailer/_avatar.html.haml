- size = local_assigns.fetch(:size, 36)
- initials = user.presence ? user.avatar_initials :  '👤'
- if user.presence && user.thumb_url
  = image_tag user.thumb_url, alt: initials, class: "base-mailer__avatar-image rounded-circle", style: "width: #{size}px; height: #{size}px", width: size, height: size
- else
  = image_tag user.avatar_initials_url(64), alt: initials, class: "base-mailer__avatar-image rounded-circle", style: "width: #{size}px; height: #{size}px", width: size, height: size
