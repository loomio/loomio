# Keep unclosed polls visible in threads

Threads now load every poll that has not closed, including polls scheduled to open later. Closed polls remain available in the thread history.

This prevents an active or scheduled poll from being hidden behind a misleading **Load 1 more** button when it is the only unloaded item.
