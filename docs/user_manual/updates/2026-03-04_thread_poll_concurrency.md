# One active poll per thread by default

Threads now default to allowing only one active poll at a time. A new thread setting, "Allow concurrent polls," can be switched on to permit multiple simultaneous polls in the same thread, matching the previous behavior.
