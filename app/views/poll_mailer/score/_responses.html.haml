.poll-mailer-score-responses.poll-mailer-common-responses
  %h3= t(:"poll_mailer.common.responses")
  %table
    - poll.stances.latest.each do |stance|
      %tr
        %td
          %table
            %td= render 'base_mailer/avatar', user: stance.participant
            %td.poll-mailer-common-responses__reason
              %strong= stance.participant.name
      %tr
        %td= render_rich_text(stance.reason, stance.reason_format)
      %tr
        %td= render 'poll_mailer/score/stance_chart', stance: stance
