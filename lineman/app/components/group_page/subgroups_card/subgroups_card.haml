.blank
  %section.subgroups-card{ng-if: 'showSubgroupsCard()', aria-labelledby: 'subgroups-card__title'}
    %h2#subgroups-card__title.lmo-card-heading{ translate: 'group_page.subgroups'}
    %ul.subgroups-card__list
      %li.subgroups-card__list-item{ng-repeat: 'subgroup in group.subgroups() track by subgroup.id'}
        .subgroups-card__list-item-logo
          %group_avatar{group: 'subgroup', size: 'medium'}
        .subgroups-card__list-item-name
          %a{lmo-href-for: 'subgroup'} {{ subgroup.name }}
        .subgroups-card__list-item-description {{ subgroup.description | words:10 }}

    %a.subgroups-card__add-subgroup-link.lmo-card-minor-action{href: '', ng-click: 'startSubgroup()', ng-if: 'canCreateSubgroups()'}
      %span{ translate: 'common.action.add_subgroup' }
