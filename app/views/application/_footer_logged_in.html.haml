.footer.container.clearfix
  - if user_signed_in?
    .left
      .dropdown
        %a.dropdown-toggle{'data-toggle' => 'dropdown', :href => '#'}
          = Translation.language(current_user.language_preference)
          %b.caret
        %ul.dropdown-menu
          - Translation::LANGUAGES.each do |language|
            %li= Translation.language_link(language)

      = link_to t(:translate, default: "help translate Loomio!"), 'https://www.loomio.org/discussions/4896'

  .footer-links
    = link_to t('footer.terms'), '#'
    = link_to t('footer.privacy'), privacy_path
    = link_to t('footer.pricing'), '#'
    = link_to t('footer.contact'), contact_path
    = link_to t('footer.about'), about_path
    = link_to t('footer.blog'), '#'  #blog_path, target: 'blank'
