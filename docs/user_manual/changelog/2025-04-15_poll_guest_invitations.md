# Improved poll invitations

When a group is set to "Anyone in the group can add members," polls in that group can now invite guests without requiring coordinator approval.

Previously, inviting non-members to vote in a poll required coordinator rights even when the group's membership settings were open. Now the poll invitation permission follows the group's guest-invitation policy.
