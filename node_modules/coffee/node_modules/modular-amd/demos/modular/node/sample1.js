/*global require */
(function () {
    "use strict";

    var modular = require("../../../");

    modular.require({
        paths: {
            "js": "../../../js"
        }
    }, [
        "js/util"
    ], function (
        util
    ) {
        console.log(util.get);
        console.log("ok.");
    });
}());
