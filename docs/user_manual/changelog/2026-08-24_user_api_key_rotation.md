# User API keys rotated

Loomio now excludes User API keys from group data exports. During this update, Loomio replaces every existing User API key so that keys contained in an earlier export no longer work.

Existing User API integrations must be updated with the replacement key from the [API access page](/profile/api_access). The rotation does not sign users out or change passwords, email credentials, or other account settings.
