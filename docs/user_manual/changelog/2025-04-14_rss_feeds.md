# RSS feeds restored

Group threads are again available as RSS feeds.

Add `.rss` to any group URL to get a feed of the group's latest threads. Feeds respect the group's privacy settings, so RSS readers can only access threads the requesting user has permission to see.

This re-establishes a feature that had been unavailable since the Vue upgrade.
