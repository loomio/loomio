.row
  .span6
    %h1= t :"edit_comment.edit_comment"
    - if can? :edit, @comment
      = simple_form_for @comment do |f|
        =f.input :body, label: false
        =f.submit t(:update), class: "btn btn-info"
