# Auto-accept organization members

Members who join a group or subgroup via an organization domain are now automatically accepted rather than queued as membership requests.

## How it works

When an instance admin configures organization domains, anyone signing up with a matching email address is treated as belonging to the organization. These members are now accepted immediately into any group that allows organization members to join, skipping the approval step. They can participate right away.
