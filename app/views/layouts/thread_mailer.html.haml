<!DOCTYPE HTML>
%html
  %head
    = stylesheet_link_tag 'email'
  %body
    .thread-mailer
      = render '/mailer_header'
      .thread-mailer__container
        = yield
      - if @link
        = render "footer", link: @link
