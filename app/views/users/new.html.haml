.page-container
  =image_tag("loomio-orange.png")
  .row
    .col-md-8.offset2
      %h2= t("new_user_form.create_an_account")
      %p &nbsp;
  .row
    .col-md-4.offset2
      = simple_form_for @user, :validate => true do |f|
        = f.input :name
        = f.input :email
        = f.input :password
        = f.submit class: "btn btn-info btn-lg", id: 'submit', :data => {:disable_with => t("helpers.submit.user.create")}
    .col-md-2.offset1
      =image_tag("mascot-medium-lefthand.gif")