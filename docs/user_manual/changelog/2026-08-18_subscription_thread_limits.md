# Subscription thread limits include standalone polls

Subscription thread limits now count every discussion thread and standalone poll thread in an organization and its subgroups, including discarded threads. Polls added to an existing discussion do not use an additional thread.

When an organization reaches its thread limit, members cannot start another discussion or standalone poll. They can still add polls to existing discussions.
