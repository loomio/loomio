= @motion.name
\
=@motion.description
\
= t(:proposed_when_by_who,
  when: time_ago_in_words(@motion.created_at),
  who: @motion.author_name)
\
= t(:positions).capitalize
- @motion.grouped_unique_votes.each do |vote|
  = "#{vote.user_name} #{vote.position_to_s}"
  = vote.statement
  \
\
= t(:engagement_on_closed_proposal, percent: @motion.percent_voted, :numerator => (@motion.voters_count), denominator:  @motion.members_count)
