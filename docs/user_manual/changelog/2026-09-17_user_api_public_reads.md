## User API reads now follow public visibility

User API keys can list public discussions and polls in publicly visible groups without the API-key user being a group member. Private groups and private topics remain restricted according to the same permissions used by the Loomio interface.
