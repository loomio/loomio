# Hide poll results until the poll closes

Poll creators can now choose to conceal results while voting is in progress.

When enabled, the results panel and vote breakdown are hidden from everyone — including the poll creator — until the poll closes. Each participant still sees their own vote. Once the poll closes, results become visible to everyone with permission to see them.

Set this in the poll creation form under "More settings." Available on all poll types.
