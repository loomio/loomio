- size = local_assigns.fetch(:size, 36)
- if user.presence
  - if user.thumb_url
    .base-mailer__avatar.v-avatar{title: user.name, style: "width: #{size}px; height: #{size}px"}
      = image_tag user.thumb_url, alt: "#{user.avatar_initials}", class: "base-mailer__avatar-image",  style: "width: #{size}px; height: #{size}px"
  - else
    .base-mailer__avatar.v-avatar.base-mailer__avatar-initials{style: "width: #{size}px; height: #{size}px"}= user.avatar_initials
- else
  .base-mailer__avatar.v-avatar.base-mailer__avatar-initials{style: "width: #{size}px; height: #{size}px"} 👤
