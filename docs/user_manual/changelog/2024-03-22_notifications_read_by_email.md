# Track notifications read via email

When you read a thread by clicking a link in a notification email, the corresponding in-app notification is now marked as read automatically. Previously, reading the thread from an email link would leave the notification unread in the app, requiring you to clear it separately.
