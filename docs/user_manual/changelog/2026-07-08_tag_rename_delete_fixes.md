# Tag rename and delete fixes

Group admins can now rename tags when only the letter case changes, such as changing `advice` to `Advice`.

Deleting a tag also handles older tag records that contain extra whitespace, so the tag is removed from the group tag list and from tagged discussions.

The tag color picker now uses the same dot style shown on tag chips.
